KurZaSoftuni = (array = []) => {
    let sum = 0;
    for(let i = 0; i < array.length; i++){
        array[i] = Number(array[i]);
    }
    forEach(element => {
        
    });(let num in arr){
        if(num % 2 === 0){
           sum += num; 
        }
        
    }
    console.log(sum);
};
KurZaSoftuni(20,30,40);